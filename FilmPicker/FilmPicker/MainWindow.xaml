<Window
    x:Class="FilmPicker.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FilmPicker"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:brushes="using:CommunityToolkit.WinUI.UI.Media"
    xmlns:VM="using:FilmPicker.ViewModels"
    xmlns:Model="using:FilmPicker.Models"
    mc:Ignorable="d"
    x:Name="mainWindow">
    <Grid x:Name="mainGrid" Padding="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="200" />
            <ColumnDefinition Width="*" MinWidth="200" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="10*" MinHeight="100" />
            <RowDefinition Height="15*" MinHeight="300" />
        </Grid.RowDefinitions>
        <Grid.Resources>
            <DataTemplate x:Key="NumberTemplate">
                <NumberBox Value="{Binding Multiplier, Mode=TwoWay}" Minimum="1" Maximum="10" SpinButtonPlacementMode="Inline" Margin="1"/>
            </DataTemplate>
            <DataTemplate x:Key="DeleteButton">
                <Button Click="DeleteButtonClick" Tag="{Binding Id}" HorizontalAlignment="Stretch" Background="DarkRed" CornerRadius="0" Margin="5 0 0 0">
                    <SymbolIcon Symbol="Delete" />
                </Button>
            </DataTemplate>
        </Grid.Resources>
        <Grid x:Name="winnerGrid" Grid.ColumnSpan="2" Grid.Row="0">
            <Button Click="PickRandomFilm_click" Background="{ThemeResource SystemAccentColorDark1}" HorizontalAlignment="Center" VerticalAlignment="Bottom" Width="250">Select random</Button>
        </Grid>
        <Grid Grid.Row="1"
              Grid.Column="0">
            <StackPanel Orientation="Vertical">

                <StackPanel Orientation="Horizontal" Margin="0 0 0 10" >
                    <Button Command="{x:Bind ViewModel.AddCustomFilmToList}" Background="Green">
                        <SymbolIcon Symbol="Add" />
                    </Button>
                </StackPanel>
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <controls:DataGrid
                        ItemsSource="{x:Bind ViewModel.Films, Mode=OneWay}"
                        AutoGenerateColumns="False"
                        x:Name="filmsGrid"
                        Background="{ThemeResource SystemChromeMediumHighColor}">
                        <controls:DataGrid.Columns>
                            <controls:DataGridTextColumn Header="Title" Binding="{Binding Title, Mode=TwoWay}" Width="3*" />
                            <controls:DataGridTemplateColumn Header="Multiplier" CellTemplate="{StaticResource NumberTemplate}" Width="*" />
                            <controls:DataGridTemplateColumn CellTemplate="{StaticResource DeleteButton}" />
                        </controls:DataGrid.Columns>
                    </controls:DataGrid>
                </ScrollViewer>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1"
              Grid.Column="1"
              Padding="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="60" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0"
                  Padding="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" PlaceholderText="Enter title" Text="{x:Bind ViewModel.SearchExpression, Mode=TwoWay}" MaxHeight="30" Margin="10 0 10 0"/>
                <Button Grid.Column="1" Command="{x:Bind ViewModel.GetSearchList}">Search</Button>
            </Grid>
            <Grid Grid.Row="1">
                <ProgressRing IsActive="{x:Bind ViewModel.IsSearching, Mode=OneWay}" Width="100" Height="100"/>
                <ListView ItemsSource="{x:Bind ViewModel.SearchFilmList, Mode=OneWay}">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="Model:SearchFilmModel">
                            <Grid Height="60">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="100" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Ellipse Grid.Column="0" Width="50" Height="50">
                                    <Ellipse.Fill>
                                        <ImageBrush Stretch="UniformToFill">
                                            <ImageBrush.ImageSource>
                                                <BitmapImage UriSource="{Binding ImageUrl}" DecodePixelType="Logical" DecodePixelWidth="50" />
                                            </ImageBrush.ImageSource>
                                        </ImageBrush>
                                    </Ellipse.Fill>
                                </Ellipse>
                                <TextBlock Grid.Column="1" Text="{Binding Title}" Style="{ThemeResource SubtitleTextBlockStyle}" VerticalAlignment="Center"/>
                                <StackPanel Grid.Column="2" Orientation="Horizontal" >
                                    <Button Margin="20 0 0 0" Click="GetSearchItemDetails" Tag="{Binding Id}" >
                                        <SymbolIcon Symbol="Help" />
                                    </Button>
                                    <Button Margin="20 0 0 0">
                                        <SymbolIcon Symbol="Add" />
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>
            </Grid>
        </Grid>
    </Grid>
</Window>
